import jwt, { SignOptions } from 'jsonwebtoken';
declare type JwtPayload = {
    destination: string;
    code: string;
    [key: string]: any;
};
export declare const decodeToken: (secret: string, token?: string | undefined) => string | jwt.JwtPayload;
export declare const generateToken: (secret: string, payload: JwtPayload, options?: SignOptions) => string;
export {};
